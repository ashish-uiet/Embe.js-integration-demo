<div class="container">
    <div class="menu">
        {{#link-to "index"}}
            <h3><em>SuperRentals</em></h3>
        {{/link-to}}
        <div class="links">
            {{#link-to "about" class="menu-about"}}
                About Us
            {{/link-to}}
            {{#link-to "contact" class="menu-contact"}}
                Contact
            {{/link-to}}
        </div>
    </div>
    <div class="body">
        {{outlet}}
    </div>    
</div>